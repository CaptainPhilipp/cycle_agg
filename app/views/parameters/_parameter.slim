p = parameter.ru_title

div.values
  - values = show_values_for(parameter)

  - if values.any?
    - present_type = ValuesPresentTypeByType.(parameter, values, :ru)
    = render "parameters/#{present_type}", values: values, parameter: parameter

  - else
    p = '-'
